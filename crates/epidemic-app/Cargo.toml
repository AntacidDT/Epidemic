[package]
name = "epidemic-app"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
epidemic-render = { path = "../epidemic-render" }
log = "0.4"
env_logger = "0.11"

[target.'cfg(target_os = "android")'.dependencies]
android-activity = { version = "0.6", features = ["native-activity"] }
